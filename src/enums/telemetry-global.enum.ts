export enum TelemetryGlobal {
  TRACER_PROVIDER = "tracer_provider",
  METER_PROVIDER = "meter_provider",
  LOGGER_PROVIDER = "logger_provider",
  PROPAGATOR = "propagator",
  CONTEXT_MANAGER = "context_manager",
}
