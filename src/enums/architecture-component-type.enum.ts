export enum ArchitectureComponentType {
  SERVICE = "service",
  FUNCTION = "function",
  GATEWAY = "gateway",
  DATABASE = "database",
  CACHE = "cache",
  QUEUE = "queue",
  CONSUMER = "consumer",
  EXTERNAL = "external",
  FRONTEND = "frontend",
  CRON = "cron",
}
